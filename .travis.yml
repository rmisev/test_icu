## https://genbattle.bitbucket.io/blog/2016/01/17/c++-travis-ci/ ##

# Enable C++ support
language: cpp

# Compiler selection
matrix:
  include:
    - os: linux
      compiler: gcc

    - os: osx
      compiler: clang

# Build steps
script:
  - mkdir build
  - cd build
  - cmake .. && cmake --build . --config Release
  - ctest -C Release -V
